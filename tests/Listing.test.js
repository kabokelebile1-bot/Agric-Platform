import React from 'react';\nimport { render, screen } from '@testing-library/react';\nimport Listing from '../src/Listing';\n\n// Mock data for the listing component\nconst mockListing = {\n    id: 1,\n    title: 'Test Listing',\n    price: 100,\n    description: 'This is a test description.',\n    image: 'https://example.com/image.png'\n};\n\ndescribe('Listing Component', () => {\n    test('renders correctly', () => {\n        render(<Listing listing={mockListing} />);\n        expect(screen.getByText(/Test Listing/i)).toBeInTheDocument();\n        expect(screen.getByText(/100/i)).toBeInTheDocument();\n        expect(screen.getByText(/This is a test description./i)).toBeInTheDocument();\n    });\n\n    test('renders image correctly', () => {\n        render(<Listing listing={mockListing} />);\n        const img = screen.getByAltText(/Test Listing/i);\n        expect(img).toHaveAttribute('src', mockListing.image);\n    });\n});